@model BudgetTracker.ViewModels.History.DetailsViewModel;
@using System.Text.Json

@{
    string mes = new DateTime(1, Model.Month, 1).ToString("MMMM", new System.Globalization.CultureInfo("es-AR"));

    var labelsIngresos = JsonSerializer.Serialize(Model.CategoriesIncomes?.Select(c => c.CategoryName) ?? Array.Empty<string>());
    var datosIngresos = JsonSerializer.Serialize(Model.CategoriesIncomes?.Select(c => c.TotalIncome) ?? Array.Empty<decimal>());

    var labelsGastos = JsonSerializer.Serialize(Model.CategoriesBills?.Select(c => c.CategoryName) ?? Array.Empty<string>());
    var datosGastos = JsonSerializer.Serialize(Model.CategoriesBills?.Select(c => c.TotalBill) ?? Array.Empty<decimal>());
}

<div class="d-flex flex-column justify-content-center align-items-center px-2">
    <div class="bt-page-title">
        <a asp-controller="History" asp-action="GetBillIncomeAndMonthlyTotal" asp-route-month="@Model.Month" asp-route-year="@Model.Year" class="btn text-white">
            <svg xmlns="http://www.w3.org/2000/svg" fill="currentColor" class="bi bi-arrow-left-circle-fill bt-icon" viewBox="0 0 16 16">
                <path d="M8 0a8 8 0 1 0 0 16A8 8 0 0 0 8 0m3.5 7.5a.5.5 0 0 1 0 1H5.707l2.147 2.146a.5.5 0 0 1-.708.708l-3-3a.5.5 0 0 1 0-.708l3-3a.5.5 0 1 1 .708.708L5.707 7.5z" />
            </svg>
        </a>
        <h1>Detalles del mes @(char.ToUpper(mes[0]) + mes.Substring(1)) - @Model.Year.ToString("D4")</h1>
    </div>

    <div class="d-flex flex-column justify-content-center align-items-center border rounded box-shadow p-3 p-md-4 bg-secondary center w-100 bt-panel-wide">
        <div class="d-flex flex-column justify-content-center align-items-center pt-2 pb-2 text-center">
            <div>
                <h2 style="font-size: clamp(1rem, 3.5vw, 1.75rem);">
                    Total de ingresos: @(Model.TotalIncome.ToString("C")) -
                    Total de gastos: @(Model.TotalBill.ToString("C"))
                </h2>
            </div>

            <div>
                <h2 style="font-size: clamp(1rem, 3.5vw, 1.75rem);">
                    Total del mes: @((Model.TotalIncome - Model.TotalBill).ToString("C", new System.Globalization.CultureInfo("es-AR")))
                </h2>
            </div>
        </div>

        <div class="bt-details-layout">
            <div class="bt-table-scroll bt-details-main-table">
            <table class="table table-bordered text-center" style="width: 100%;">
                <thead>
                    <tr>
                        <th>Fecha</th>
                        <th>Descripción</th>
                        <th>Tipo</th>
                        <th>Categoria</th>
                        <th>Monto</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Transactions != null && Model.Transactions.Any())
                    {
                        @for (var numDay = 1; numDay <= DateTime.DaysInMonth(Model.Year, Model.Month); numDay++)
                        {
                            var currentDate = new DateOnly(Model.Year, Model.Month, numDay);
                        
                            @if (Model.Transactions.Any(t => t.Date == currentDate))
                            {
                                @foreach (var transaction in Model.Transactions.Where(t => t.Date == currentDate))
                                {
                                    <tr>
                                        <td>@transaction.Date.ToString("dd/MM/yyyy")</td>
                                        <td>@transaction.Description</td>
                                        <td>@transaction.Type</td>
                                        <td>@transaction.Category</td>
                                        <td>@transaction.Amount.ToString("C")</td>
                                    </tr>
                                }
                            }
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="5">No hay entradas para este mes.</td>
                        </tr>
                    }
                </tbody>
            </table>
            </div>

            <div class="bt-details-side">
                <div class="d-flex flex-column align-items-center w-100" style="max-width: 100%;">
                    <p class="text-center mb-0 pb-0 border-bottom-0 border-top border-start border-end" style="width: 100%;">
                        <strong>Ingresos</strong>
                    </p>
                    <div class="bt-table-scroll w-100">
                    <table class="table table-bordered text-center" style="width: 100%;">
                        <thead>
                            <tr>
                                <th>Categoria</th>
                                <th>Monto</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (Model.CategoriesIncomes != null && Model.CategoriesIncomes.Any())
                            {
                                @foreach (var category in Model.CategoriesIncomes)
                                {
                                    <tr>
                                        <td>@category.CategoryName</td>
                                        <td>@category.TotalIncome.ToString("C")</td>
                                    </tr>
                                }
                            }
                            else
                            {
                                <tr>
                                    <td colspan="2">No hay ingresos para este mes.</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    </div>
                    <p class="text-center mb-0 pb-0 border-bottom-0 border-top border-start border-end pt-1" style="width: 100%;">
                        <strong>Gastos</strong>
                    </p>
                    <div class="bt-table-scroll w-100">
                    <table class="table table-bordered text-center" style="width: 100%;">
                        <thead>
                            <tr>
                                <th>Categoria</th>
                                <th>Monto</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (Model.CategoriesBills != null && Model.CategoriesBills.Any())
                            {
                                @foreach (var category in Model.CategoriesBills)
                                {
                                    <tr>
                                        <td>@category.CategoryName</td>
                                        <td>@category.TotalBill.ToString("C")</td>
                                    </tr>
                                }
                            }
                            else
                            {
                                <tr>
                                    <td colspan="2">No hay gastos para este mes.</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    </div>
                </div>
                <div class="d-flex flex-column align-items-center mb-3 w-100" style="min-height: 12rem;">
                    @if (Model.CategoriesIncomes != null && Model.CategoriesIncomes.Any())
                    {
                        <div class="d-flex flex-column align-items-center mb-2 w-100" style="height: 12rem; max-width: 16rem;">
                            <p class="mb-0"><strong>Distribucion de Ingreso</strong></p>
                            <div style="position: relative; width: 100%; height: 10rem;">
                                <canvas id="chartIngresos" style="width: 100%; height: 100%;"></canvas>
                            </div>
                        </div>
                    }

                    @if (Model.CategoriesBills != null && Model.CategoriesBills.Any())
                    {
                        <div class="d-flex flex-column align-items-center pt-2 w-100" style="height: 12rem; max-width: 16rem;">
                            <p class="mb-0"><strong>Distribucion de Gastos</strong></p>
                            <div style="position: relative; width: 100%; height: 10rem;">
                                <canvas id="chartGastos" style="width: 100%; height: 100%;"></canvas>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>


@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    
    <script>
        document.addEventListener("DOMContentLoaded", function () {
        const legendColor = document.documentElement.dataset.theme === 'dark' ? '#e9ecef' : '#212529';
        
        const canvasIngresos = document.getElementById('chartIngresos');
        if (canvasIngresos) {
            const ctxIngresos = canvasIngresos.getContext('2d');
            new Chart(ctxIngresos, {
                type: 'doughnut',
                data: {
                    labels: @Html.Raw(labelsIngresos),
                    datasets: [{
                        data: @Html.Raw(datosIngresos),
                        backgroundColor: ['#2ecc71', '#27ae60', '#16a085', '#1abc9c', '#3498db'],
                        borderWidth: 1
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: { 
                            position: 'bottom',
                            maxHeight: 60,
                            labels: {
                                color: legendColor,
                                font: {
                                    size: 18,
                                    weight: 'bold'
                                },
                                boxWidth: 16,
                                boxHeight: 16,
                                padding: 8
                            }
                        }
                    },
                    layout: {
                        padding: {
                            top: 0,
                            bottom: 0
                        }
                    },
                    radius: '90%',
                    cutout: '30%'
                }
            });
        }

        const canvasGastos = document.getElementById('chartGastos');
        if (canvasGastos) {
            const ctxGastos = canvasGastos.getContext('2d');
            new Chart(ctxGastos, {
                type: 'doughnut',
                data: {
                    labels: @Html.Raw(labelsGastos),
                    datasets: [{
                        data: @Html.Raw(datosGastos),
                        backgroundColor: ['#e74c3c', '#c0392b', '#e67e22', '#d35400', '#f1c40f'],
                        borderWidth: 1
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: { 
                            position: 'bottom',
                            maxHeight: 60,
                            labels: {
                                color: legendColor,
                                font: {
                                    size: 18,
                                    weight: 'bold'
                                },
                                boxWidth: 16,
                                boxHeight: 16,
                                padding: 8
                            }
                        }
                    },
                    layout: {
                        padding: {
                            top: 0,
                            bottom: 0
                        }
                    },
                    radius: '90%',
                    cutout: '30%'
                }
            });
        }
    });
    </script>
}