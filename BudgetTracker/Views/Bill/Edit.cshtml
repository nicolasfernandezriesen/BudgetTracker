@model BudgetTracker.ViewModels.Bill.EditViewModel

<div class="d-flex flex-column justify-content-center align-items-center bt-viewport-center px-2" style="height: 65vh;">

    <div class="bt-page-title">
        <h1>Editar gasto</h1>
    </div>

    <div class="border rounded box-shadow p-4 m-2 m-md-4 bg-secondary center bt-panel">
        <form asp-controller="Bill" asp-action="Edit" id="editBillForm">
            <div class="p-2">
                <div class="form-group text-center">
                    <label asp-for="BillsAmount" class="form-label pb-1">Monto del gasto: </label><br />
                    <input asp-for="BillsAmount" class="form-control text-center bt-form-narrow-input" type="number" name="BillsAmount" value="@Model.BillsAmount" min="0" required />
                </div>
                <div class="form-group text-center">
                    <label asp-for="CategoryId" class="form-label pt-2 pb-1">Categoria (Seleccionar uno): </label>
                    <div class="d-flex flex-wrap bt-category-wrap">
                        <select id="category-select" asp-for="CategoryId" name="CategoryId" class="form-control" placeholder="Selecciona o busca...">
                            <option value="">Buscar categoría...</option>
        
                            @foreach (var group in Model.AvailableCategories)
                            {
                                <optgroup label="@group.GroupName">
                                    @foreach (var sub in group.SubCategories)
                                    {
                                        <option value="@sub.CategoryId">@sub.CategoryName</option>
                                    }
                                </optgroup>
                            }
                        </select>
                    </div>
                </div>
                <div class="form-group text-center">
                    <label asp-for="BillsDesc" class="form-label pt-2 pb-1">Descripcion (opcional): </label>
                    <textarea asp-for="BillsDesc" class="form-control" name="BillsDesc" value="@Model.BillsDesc"></textarea>
                </div>
                <div class="form-group pt-2 text-center">
                    <label asp-for="BillsDate" class="form-label pt-2 pb-1 pe-4">Fecha: </label><br />
                    <input asp-for="BillsDate" type="date" name="BillsDate" class="form-control text-center bt-form-narrow-input" min="@DateTime.Now.AddMonths(-2).ToString("yyyy-MM-dd")" max="@DateTime.Now.AddMonths(2).ToString("yyyy-MM-dd")" required />
                </div>
            </div>
            <input type="hidden" name="id" value="BillId" />
            <div class="d-flex justify-content-evenly align-items-center pt-2 flex-wrap gap-2">
                <button type="button" class="btn btn-danger" onclick="Cancel()">Cancelar</button>
                <button type="button" class="btn btn-primary" onclick="SaveEdit()">Guardar gasto</button>
            </div>
            <input asp-for="BillId" type="hidden" />
        </form>
    </div>
</div>

<div id="billDate" data-bill-date="@Model.BillsDate.ToString("yyyy-MM-dd")"></div>

@section Scripts {
    <script src="~/js/Bill/Edit.js" asp-append-version="true"></script>
    <link href="https://cdn.jsdelivr.net/npm/tom-select/dist/css/tom-select.bootstrap5.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/tom-select/dist/js/tom-select.complete.min.js"></script>

    <script>
        new TomSelect("#category-select", {
        create: false,
        sortField: { field: "text", direction: "asc" },
        placeholder: "Escribe para buscar...",
        plugins: ['dropdown_input']
        });
    </script>
}
