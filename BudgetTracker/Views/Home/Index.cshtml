@{
    ViewData["Title"] = "Login Page";
}

@inject Microsoft.AspNetCore.Authentication.IAuthenticationService authenticationService

@{
    if (User.Identity.IsAuthenticated) // If the user is already authenticated, redirect to the User controller
    {
        <script>window.location.href = '@Url.Action("Index", "User")';</script>
    }
}

<div class="d-flex justify-content-center align-items-center bt-viewport-center" style="height: 50vh;">
    <div class="border rounded box-shadow p-4 bg-secondary bt-auth-panel">
        <form asp-controller="Home" asp-action="Create" method="get">
            <div class="form-group">
                <button type="submit" class="btn-home btn-primary btn-lg mb-3 btn-home-landing">Crear Usuario</button>
            </div>
        </form>

        <form asp-controller="Home" asp-action="Login" method="get">
            <div class="form-group">
                <button type="submit" class="btn-home btn-primary btn-lg btn-home-landing">Iniciar Sesion</button>
            </div>
        </form>
    </div>
</div>
